package #(baseModelPackageName);
import #(myMap.modelPackageName).#(tableMeta.modelName);
import org.springframework.data.jpa.repository.JpaRepository;
/**
 * Generated by generator.
 */
 #for(cm : tableMeta.columnMetas)
     #if (tableMeta.primaryKey.equals(cm.name))
     #set(b = cm.javaType)
     #end
 #end
public interface #(tableMeta.modelName)Dao extends JpaRepository<#(tableMeta.modelName), #(b)>{
}
