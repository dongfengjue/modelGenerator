package #(modelPackageName);

import javax.persistence.*;
import lombok.Data;

/**
 * Generated by generator
 */
@Entity
@Data
@Table(name = "#(tableMeta.name)")
public class #(tableMeta.modelName){

#for(cm : tableMeta.columnMetas)
    #if (tableMeta.primaryKey.equals(cm.name))
    @Id
    #else
    @Column
    #end
    private #(cm.javaType) #(firstCharToUpperCase(cm.attrName));

#end
}
